You are a professional sports analyst. Analyze the upcoming football match and provide a prediction for Both Teams to Score (BTTS).

MATCH DATA (JSON format):
{{MATCH_DATA}}

Teams: {{HOME_TEAM}} vs {{AWAY_TEAM}}

TASK:
Analyze the scoring form of both teams. Consider:
1. Average goals scored and conceded per match (GF/GA).
2. How often each team scores and concedes in their recent matches.
3. Defensive and offensive strength of both teams.

Provide a prediction in the following JSON format:

{
  "bothTeamsToScoreYes": number, // Probability percentage (0-100)
  "bothTeamsToScoreNo": number, // Probability percentage (0-100)
  "expectedHomeGoals": number, // Predicted goals for {{HOME_TEAM}}
  "expectedAwayGoals": number, // Predicted goals for {{AWAY_TEAM}}
  "recommendation": string, // One of: "Both Teams To Score", "Both Teams To Score - No"
  "confidence": number, // 0-100
  "reasoning": string // Brief explanation in {{LANGUAGE}} (MAX 2 sentences), citing stats like "Chelsea averages 3.0 goals..."
}

CRITICAL: Return ONLY the JSON object. No markdown formatting.
